# プロジェクト概要

**「ちかく」** — 位置情報ベースの「すれ違い」機能に特化した、恋愛に縛られないマッチングアプリ。

- 現状は `index.html` 1ファイルで完結したプロトタイプ（HTML/CSS/JS、ビルド不要）
- **学園祭版** `fes/index.html`（`/fes/`）がある。通常版を複製して改変したもので、データは `fes_` 付きの別テーブル。**2026-09-15からトップURLは多摩キャン版 `/tama/` に転送**している（`vercel.json`。学園祭版は待機中で、`/fes/` を直接開けば今も使える）
- **多摩キャン版** `tama/index.html`（`/tama/`）がある。学園祭の前に学食のQRから入ってもらう簡易実験（学園祭のデータの保険）用で、学園祭版を複製して改変したもの。データは `tama_` 付きの別テーブル（`supabase/tama_tables.sql`）
- バックエンドは Supabase（匿名認証でユーザーIDを発行、URL/anonキーはコード内に直書き）
- 主な機能: オンボーディング（絵文字アバター・タグ・興味関心選択）、現在地取得と近くのユーザー一覧・地図表示、NGワードフィルタによる不適切投稿の簡易ブロック
- 本番URL: https://chikaku-hosei.vercel.app （Vercel）
- リポジトリ: https://github.com/yamamoto0728/-

## チーム構成

- **アプリ開発陣**
  - **あお（水原、blue.aochan03@gmail.com）** — 開発（Claude Codeと一緒に作業）
  - **しゅうすけさん（山本、GitHub: yamamoto0728）** — リポジトリオーナー、PM（進行管理・ゲート判定・`main`へのマージ承認）
- **アイデア班** — りく・りこ・ずなさん・はなちゃん（企画・渉外・広報・データ分析。班の中の役割分担は2026-09-15のMTGで決める）
- ⚠️ 「山本さん」＝しゅうすけさん、「水原さん」＝あお。古い資料では別人のように書かれていることがある
- ロードマップと議事録はリポジトリの外（`J:\ちかく\ちかく_ロードマップ_2026.md`、`J:\ちかく\議事録_2026-09-15_MTG.md`、それぞれPDFあり）

# 引継ぎノート（最新セッションの状態）

新しいセッションでこのリポジトリを開いたら、まずこのセクションを読む。過去の詳細な履歴は下の「進捗ログ」を参照。

## 今回やったこと（2026-09-15）— 多摩キャン版 `/tama/` を作って公開し、位置情報をキャンパス内だけにし、地図の改善と「友達・いまどこ」まで実装

**このセッションの成果はすべて `main` にマージ済み（PR #16, #17）・Supabase側も適用済み・本番URLで反映を確認済み。残っているのは、実機での動作確認と、多摩キャン版の中身を学食向けに直すこと。**

### 1. 多摩キャン版 `/tama/` を公開（PR #16）

- 目的: **学園祭の前に、学食にQRを貼って入ってもらう簡易実験**（学園祭のデータの保険）。多摩キャンパス内でマッチングする
- あおと相談し、「同じリンクで切り替え」でも「別リポジトリ」でもなく、学園祭版と同じく**同じリポジトリの別URL**にした（データの混ざり・分岐の増加・Supabaseの二重管理を避けるため）
- `fes/index.html` を複製して `tama/index.html` に。データは `tama_` 付きの別テーブル（`supabase/tama_tables.sql` で `fes_` テーブルから属性の列・RLS込みで11個複製、実行済み）
- 地図の形 `/fes/campus-geo.js` は共用。**`fes/` を消すと多摩キャン版の地図も壊れる**
- **トップURL（`/`・`/index.html`）の転送先を `/fes/` → `/tama/` に変更**（`vercel.json`）。学園祭版は待機中で、`/fes/` を直接開けば今も使える（`alwaysOpen:true`）
- 版の切り替えはアプリ内には無く、**配るURLと `vercel.json` の転送先で決まる**（使う人は変えられない）
- ⚠️ 今日の目的の選択肢・2択のお題・出店の広告・開催時間バーなどは**学園祭向けのまま**

### 2. 位置情報の機能はキャンパス内だけ（PR #16、学園祭版・多摩キャン版とも）

- きっかけ: 自宅の町名が画面に出ていた。あおの意図は「表示を隠す」ではなく「**位置の機能をキャンパス内だけで動かす**」
- `FES.campusOnly` の意味を「外では画面ごとふさぐ」から「**外では位置の機能だけ止める**」に変え、`true` にした
- キャンパス外では: 位置を送らない・他の人の位置を取らない・近くの人の一覧と地図に出ない・すれ違いを記録しない・「近くにいます」通知と出店の判定をしない・町名や座標を出さない。**プロフィール・マッチ・チャット・コミュニティは外でも使える**
- キャンパスから出た瞬間に `profiles.sharing=false` を保存（最後のキャンパス内の位置が他の人の一覧・参加人数・通知に使われないように。戻ると `publishMe` が送り直す）
- 町名を調べる Nominatim への座標送信をやめた（場所名は「多摩キャンパス」固定）
- メンバーが家で地図・すれ違いを試すには、一時的に `FES.campusOnly:false` にする

### 3. 多摩キャン版の地図（PR #17）

- **航空写真をやめてイラストだけ**に（`campus-geo.js` が読めない時だけ航空写真で代わりに映す）
- イラストの改善: 色のコントラスト、歩道のふち、建物の影、**名前のある建物は青・食堂はオレンジ**、駐車場は点線
- **建物名を引いた状態から表示**: 名前どうしが重ならない分だけ優先度順（食堂→主要な建物→その他）に出す `layoutLabels()`。拡大するほど増え、文字も大きくなる
- **＋／－／🏫（キャンパス全体）ボタン**、左下の「⤢ 大きく」で画面いっぱい、ズーム0.5段階・上限20、地図の高さ360px

### 4. 友達と「いまどこ（建物・教室）」（PR #17、多摩キャン版だけ）

- あおの希望（Whoのような機能）。仕様はあおに質問して決めた:
  - 友達は**申請→承認**（プロフィールのシートに「友達申請する／承認する／申請中／友達をやめる」、申請と承認はプッシュ通知）
  - 友達に見せる範囲は**本人が選ぶ**（地図の位置も見せる／建物と教室だけ／友達にも見せない）
  - 建物は位置から自動判定し、**入ったら「いまどこ？」で教室・ひとことを聞く＋いつでも編集**
  - 教室・ひとことは**建物を出るか90分で消える**（`FES.roomExpireMin`）
- ナビの「招待」タブを**「友達」タブ**に置き換え（いまの自分・公開範囲・届いた申請・友達の一覧・送った申請。招待QRは下に移動）
- 「地図の位置も見せる」友達は、近くの人の地図に緑のふち＋名前で出る（共通点がなくても）
- 建物の判定は `campus-geo.js` の建物・グラウンドの形に `CAMPUS_LABELS` の名前を付けた21か所（門は除外）。GPSのふらつき対策で、**入る判定は20秒、出る判定は3分**同じ状態が続いた時（`FES.placeEnterSec/placeLeaveSec`）、精度50mより悪い位置は無視
- データ: `supabase/tama_friends.sql`（**実行済み**、ポリシー数 tama_friends=4 / tama_presence=4 を確認）

### 5. 開発環境

- あお個人の Claude Code 設定で、ファイル編集・コマンドを確認なしで実行するようにした（`J:\ちかく\.claude\settings.local.json` と `yamamoto0728-original/.claude/settings.local.json`。後者は `.git/info/exclude` でgitに載らない）
- ただし共有設定 `.claude/settings.json` の `ask`（削除・`git push`・`gh pr create/merge`・DB破壊系）は個人設定から外せないので、引き続き確認が出る

**設計上の判断メモ**:
- 教室まで分かる情報なので、誰でも読める `profiles` ではなく別テーブル `tama_presence` にし、**RLSで本人と承認済みの友達だけが読める**ようにした（「友達にも見せない」の人は友達からも読めない）
- 「友達にも見せない」は友達向けの設定。「近くの人」への表示は従来どおり「位置情報を共有」で決まる（画面に注記あり）
- **ブラウザは閉じている間の位置を取れない**ので、建物の出入りはアプリを開いている間だけ判定。友達の画面では「◯分前」を出し、最後の保存から90分たったら出さない
- 画面の確認は、本番DBに試しのユーザーを作らないよう、**supabase-js を「何を聞いても空で返す」偽物に差し替えたローカルサーバー＋ヘッドレスChrome**で行った（手順はこのセッションのスクラッチにあり、リポジトリには無い）

## ブロッカー・待ち状態

- **コード・DBは揃っていて、技術的なブロッカーはなし**
- **実機確認待ち（あお・メンバー）**: 多摩キャン版を2台以上・多摩キャンパスで（下の「次回やること」）
- **9/15(火) MTGの決定がまだコードに反映されていない**（議事録も未作成）: ①2択のお題と問数 ②メッセージ機能の範囲 ③「おおまかな位置」スイッチの扱い ④選択肢の追加・削除 ⑤アイデア班の中の役割分担
- **アイデア班（渉外）待ち**: 出店をアプリで宣伝してよいか、学園祭①②の日時
- **アイデア班（データ）待ち**: アンケート（Googleフォーム）のURL
- 多摩キャンパスでのGPS実測はまだ（すれ違い50m、建物判定の20秒／3分は仮値）

## 次回やること

### まず最初に: 多摩キャン版の実機確認（2台以上、多摩キャンパスで）

1. 家で開くと「キャンパスの外にいます」と出て、プロフィール・マッチ・チャットは使える
2. キャンパス内で地図・近くの人の一覧・すれ違いが動く
3. 友達申請→承認→建物に入って「いまどこ？」に教室を入れる→相手の「友達」タブに「社会学部A棟・401」と出る→「地図の位置も見せる」で相手の地図に緑のふち
4. 建物に入ったと判定されるまでの時間（20秒）と、教室の中でGPSがずれて勝手に「出た」にならないか（3分）→結果で `FES.placeEnterSec/placeLeaveSec` を調整
5. 地図の操作感（ピンチ・＋／－・全画面）と、建物名の位置が実際と合っているか
- 1台だけで試すなら「近くの人」の「🧪 1台で動作確認する」→テスト用の人に友達申請すると、その場で友達になり「社会学部A棟・401」にいる表示になる

### 次に: 多摩キャン版の中身を学食向けに直す（あおの指示待ち）

- `tama/index.html` の `FA`（今日の目的の選択肢・2択のお題）が学園祭向けのまま
- 出店の広告・開催時間バー（「メンバー動作確認中」）を残すか
- 学食に貼るQRは **`/tama/` を直接入れる**（トップURLを後で学園祭版に戻しても貼り直し不要）
- 実験期間が決まったら `FES.alwaysOpen:false` と `FES.sessions` を入れる（手で `/fes/` を開かれるのを防ぐなら学園祭版も同様）

### 9/15 MTGの決定を反映する（学園祭版）

- 2択のお題 → `FA.questions`、選択肢 → `FA` の各配列
- メッセージ機能の範囲（今は24時間で期限切れ・メッセージ解禁は課金デモ）
- 「おおまかな位置」スイッチ（約100mにぼかすので、50mの判定・出店・建物の判定が効かなくなる）を隠すか

### 学園祭が近づいたら（切り替えチェックリスト）

- [ ] `vercel.json` の転送先を `/tama/` → `/fes/` に戻す
- [x] `FES.campusOnly` → `true`（2026-09-15に変更済み。外では位置の機能だけ止まる）
- [ ] `FES.alwaysOpen` → `false`、`FES.sessions` に本番日時
- [ ] `FES.surveyUrl` にアンケートURL
- [ ] `FES.encounterRangeM` をGPS実測の結果で調整
- [ ] 出店を `fes_booths` に登録（宣伝の許可が出た場合）
- [ ] 時間で送る通知を `fes_scheduled_pushes` に登録
- [ ] 前日に多摩キャンパスで、開催時間に合わせて全員で動作確認

### 学園祭・実験が終わったら

- `select cron.unschedule('fes-broadcast');` で1分ごとの自動実行を止める
- `vercel.json` を消してトップURLを通常版に戻す
- `fes_` / `tama_` テーブル（`tama_friends`・`tama_presence` を含む）のデータを書き出してから削除（各SQLの末尾。利用規約で「実験終了後に速やかに削除」としている）

### 通常版の今後（学園祭の後に。2026-08-02に議論済み）

- 期限付きコミュニティ（数日で消える部屋。少人数でも成立し、過疎部屋が残らない）
- 場所ゲート（その場所に居た人だけ入れる部屋。店舗単位の粒度の技術検証が先）

## 未解決の疑問点

- **友達・教室の情報を研究でどう扱うか**: 利用規約・同意画面の説明は位置の共有まで。友達関係や教室の記録を集めることを追記する必要があるか、アイデア班（データ）と要相談
- 友達機能を学園祭版にも入れるか（今は多摩キャン版だけ）
- **すれ違い等の実験データが3か所に分かれた**: 〜9/13は `encounters`（500m）、9/13〜9/15は `fes_encounters`（50m）、9/15以降はトップURLが多摩キャン版なので `tama_encounters`（50m）。分析方法をアイデア班と相談
- 「おおまかな位置」スイッチの扱い（MTGで決める）
- 「共通点のある人だけ表示」で表示0人の人が多くなりうる（多摩キャン版も同じ）。判定を緩めるか（`FA.weak`・`encounterRangeM`）
- 実機で**未確認**: 近くにいます通知・時間で全員に通知・友達申請の通知が実際に届くか、地図の操作感、建物判定の精度
- ロードマップの【要確認】が多い（G1・G2の結果、9月の第1回実験の実施状況、発表日、学園祭の日時）
- 持ち越し: 部屋作成¥500の課金設計（しゅうすけさんと要相談）、コミュニティ通知が宛先ごとにEdge Functionを1回ずつ呼ぶ実装

# リポジトリの扱い方

このフォルダ（`yamamoto0728-original/`）は GitHub 上の原本と `.git` でつながった実クローン。**`main` ブランチを直接編集しない**。作業は必ずブランチを切って行う。

```
git checkout -b feature/○○
```

複数メンバーが同時に作業しても、ブランチを分けていればお互いの変更を壊さない。他メンバーが先に `main` を更新していた場合は作業ブランチ側で追従する:

```
git fetch origin
git rebase origin/main   # 衝突した場合だけ手動で解消
```

# 「マージして」と指示されたときのClaudeの役割

ユーザーから「マージして」と指示されたら、Claudeは以下を行う（**PR作成まで**。実際のmainへの取り込みはユーザー/チームが手動で行う）:

1. `git add` → `git commit`（変更内容を記録、メッセージは変更内容から作成）
2. `git push origin <作業ブランチ名>`
3. `gh pr create --base main`（Pull Requestを自動作成）
4. 作成したPRのURLを報告して完了

**Claudeはここで止まる。mainへの自動マージ・強制的な衝突解決は行わない。**

## ⚠️ PRのbaseは必ず `main` にする（2026-08-02の失敗から）

**未マージのPRの上に別のPRを積む（stacked PR）ことはしない。** 一度これをやって事故が起きた:

- PR #9 の base を、まだマージされていない `feature/like-push-notify` に設定した
- 先に PR #8 が `main` にマージされ、そのあと PR #9 がマージされた
- その結果 **PR #9 の中身は `feature/like-push-notify` に入っただけで `main` に届かず**、GitHub上は「Merged」と表示されるのに本番には反映されない状態になった
- 回収のために PR #10 を追加で作る羽目になった

前のPRの成果物に依存する作業をする場合でも、**baseは`main`のままにして、前のPRがマージされてから `git rebase origin/main` で追従する**。

## そのあとユーザーがやること

1. 提示されたPRのURLを開き、差分を確認する
2. 必要ならチームメンバーにレビューを依頼する
3. 問題なければGitHub上で「Merge pull request」を押す（これでようやく`main`＝原本に反映される）
4. コンフリクトが出ていたら、その内容をClaudeに伝えて一緒に解消する

## 前提条件

PR作成には `gh` CLI のログインが必要。**2026-08-02時点で認証済み（アカウント: blueaochan03-spec）で、Claudeが`gh pr create`まで実行できることを確認済み**。過去の引継ぎに「`gh`がこの環境に無いのでPR作成はあおが手動」と書かれていたが、それは解消されている。

## あおに操作をお願いするときの注意

**SQLやコマンドを実行してもらうときは、ファイルパスではなく中身をそのまま貼り付けて渡すこと。** 「`supabase/room_members.sql` を実行して」と伝えたところ、SQL Editorにファイルパスの文字列が貼られて `syntax error at or near "supabase"` になった実例がある。コピペできる完全な形で提示する。

# 「今日はここまで」と指示されたときのClaudeの役割

ユーザーが「今日はここまで」「一旦ここで終わり」など作業終了の合図を出したら、Claudeはこのファイル冒頭の「引継ぎノート」セクションを**上書き**して、次回セッションが読むだけで迷わず再開できる状態にする。

引継ぎノートに書く内容:

1. 今回のセッションでやったこと（要約でよい、詳細は進捗ログ参照でOK）
2. 今ブロックされていること・誰の返事/操作待ちか（例: 「山本さんのSupabase招待待ち」のように、待っている相手と対象を具体的に）
3. 次回再開したらまず何をするか
4. 未解決の疑問点・判断が必要なこと

進捗ログ（下のセクション）とは違い、引継ぎノートは追記ではなく**常に最新状態だけを残す**。これにより、次のセッションが長い進捗ログを遡って推測する必要がなくなる（今回、「山本さんに権限をもらう」がどのサイトの話か特定できず聞き直しになったのが再発防止のきっかけ）。

# 進捗ログ

ユーザーが指示するたびに、日付＋一言ログをこの下に追記していく（履歴として残す。上書きしない）。

- 2026-07-15: プロジェクトの複製・ブランチ運用・「マージして」で動く自動PR作成の仕組みを構築。CLAUDE.mdを作成し、次にこのリポジトリを開くClaudeが仕組みとプロジェクト概要を把握できるようにした。
- 2026-07-15: `feature/matching-tweak`ブランチでCLAUDE.mdをcommit→push→PR化（PR #1, https://github.com/yamamoto0728/-/pull/1 ）。ユーザーはこのリポジトリにwrite権限あり、マージボタンは押せることを確認済み。PRはまだ未マージ。
- 2026-07-15: `.claude/settings.json`を新規作成。開発系操作（ファイル編集・npm/pipインストール・lint/test・gitの読み取り系とadd/commit・ghの読み取り系）は自動承認、削除・push・PR作成/マージ・DB破壊系操作は`ask`のまま残す方針で設定。「アプリを起動してブラウザで確認する」操作（runスキル、ローカルサーバー起動）も自動承認に追加。ただし「Bashコマンド全体を無条件で自動承認」は、削除・push・DB操作などのask設定を素通りしてしまうリスクがあるため見送った。設定変更後もセッション内でプロンプトが出続けたため、IDE（Cursor）再起動で設定を読み直す必要がある可能性が高い。
- 2026-07-15: 「リンクに入った人の情報を記憶する」機能を実装。既存の匿名認証（supabase-jsのデフォルトlocalStorage永続化）自体は同一ブラウザなら`myId`を再利用できていたが、`profiles`テーブルから過去のプロフィールを読み戻す処理がなく、毎回オンボーディング画面が出ていたのが原因。`tryResumeSession()`を追加し、boot時に`myId`で`profiles`を検索、既存プロフィールがあればオンボーディングをスキップして`near`画面に直行するようにした（index.html）。制約として、匿名認証はブラウザのlocalStorageに紐づくため「同一端末・同一ブラウザ」でのみ記憶が有効（別端末では非対応、ユーザー確認済みでこの範囲でOKとの合意）。この環境にNode/Python/ブラウザ自動化ツールが無く実機でのブラウザ動作確認はできておらず、コードレビューのみで実装。ユーザー側での手動確認待ち。
- 2026-07-15: マッチ時にスマホへプッシュ通知を送る機能を追加（PWA化）。Web Push API + manifest.json/sw.jsでPWA化し、マッチ確定時にSupabase Edge Function `send-match-push` を呼んで購読済み端末に通知する仕組み（`supabase/functions/send-match-push/index.ts`, `supabase/push_subscriptions.sql`）。Supabase側（テーブル・Edge Function・VAPID鍵）は山本さんがダッシュボードで設定済み。その後、通知が実際に届いているか怪しかったため、`notifyMatchPush`の呼び出し結果をtoast/alertで可視化するデバッグ表示を追加（原因調査中、未解決）。
- 2026-07-16: プッシュ通知が届かない原因調査を進めるには、あお自身がSupabaseダッシュボード（プロジェクト参照: `rosgvnxqcuyenlipakck`）でEdge Functionsのログやsecretsを見られる権限が必要と判明。山本さんにSupabase組織のメンバー招待（Developerロール、blue.aochan03@gmail.com宛）を依頼することにした。招待待ち、権限が付与され次第デバッグ再開。
- 2026-07-16: 招待が通り、あおが初めてSupabase SQL Editor/Edge Functionsを操作。Edge Function `send-match-push`とVAPID Secretsが実際には未設定だったことが判明し、両方セットアップし直した（PR #3, https://github.com/yamamoto0728/-/pull/3 、マージ済み）。それでも通知が届かず調査を継続、`push_subscriptions`へのinsertがRLSポリシー違反で失敗していることが判明（PR #4, #5, https://github.com/yamamoto0728/-/pull/4 , https://github.com/yamamoto0728/-/pull/5 、マージ済み）。`myId`と`auth.uid()`は一致・ポリシー定義自体も正しいことを確認済みだが、根本原因は未特定のまま時間切れ。詳細は上の「引継ぎノート」参照。
- 2026-07-24: RLSエラーの根本原因を特定。SQL Editorでの検証（ポリシー同一・トリガー無し・なりすまし`auth.uid()`でのINSERT成功）でDB側は正常と確認し、アプリ側にデバッグ追加（`feature/push-debug3`、マージ済み）。結果`sub==myId? true`・`profiles書込テスト=OK`なのにpushだけ403 → **push_subscriptionsにSELECTポリシーが無く、PostgREST経由の`.upsert()`がRLSで弾かれていた**のが原因と判明。修正はSELECTポリシー追加（`create policy ... for select using (auth.uid()=id)`）。あおの実機テストでの最終確認待ちで中断。詳細は上の「引継ぎノート」参照。
- 2026-08-02: PR #9 を base=`feature/like-push-notify` の stacked PR にしたことで事故が発生。PR #8 が先に main にマージされた後に #9 がマージされたため、**#9 の中身が main に届かず feature ブランチに取り残された**（GitHub上は「Merged」表示なのに本番未反映）。PR #10 で回収し main への反映を確認。再発防止として「PRのbaseは必ずmain、依存があっても後からrebaseで追従する」をCLAUDE.mdの運用ルールに明記。あわせて、`gh` CLIは認証済みでClaudeがPR作成まで実行できること（過去の引継ぎの記述が古かった）と、あおにSQL実行を頼むときはファイルパスではなく中身を貼って渡すこと（パスをSQL Editorに貼られてエラーになった実例あり）も追記。
- 2026-08-02: コミュニティ機能の土台を整備（`feature/community-membership`）。差別化の方向性を議論し、すれ違い＝Happn・恋だけじゃない＝Bumble/Yentaで既出と確認した上で、ちかくの独自資産は`encounters`の「反復回数＋場所」＝生活圏の一致であり、売るべきは「また会える（未来）」だと結論。その第一歩としてコミュニティを深めることにし、`room_members`テーブル新設・参加/退出・未読バッジ・コミュ投稿のプッシュ通知・N+1クエリ解消・メッセージへのNGワードフィルタ適用を実装。あわせて`enablePush()`に残っていたRLSデバッグコード（JWTデコードと毎回走る`profiles`診断書き込み）を撤去し、`alert()`を全廃。あおのSQL実行待ち。詳細は上の「引継ぎノート」参照。
- 2026-08-02: あおがSupabase SQL EditorでSELECTポリシーを追加し、通知トグルONが成功することを実機確認（RLS問題は解決確定）。mainが山本さんの新機能（マッチ24h無効化・地図縮小・コミュ情報表示）で進んでいたため取り込んだ上で、`feature/like-push-notify`ブランチを作成。`supabase/push_subscriptions.sql`にSELECTポリシーを記録として追記、デバッグ用alert表示を撤去して`notifyMatchPush`を汎用の`notifyPush(uid,title,body)`に整理、**片想いいいね時にも相手へプッシュ通知を送る新機能**を追加（送信者名は伏せる設計）。あおの実機確認・マージ指示待ち。
- 2026-09-13: 学園祭の実証実験用に**学園祭版**を作成（`feature/fes-edition`、未コミット）。通常版（`index.html`）は一切変更せず、`fes/index.html`（`https://chikaku-hosei.vercel.app/fes/` で配信する想定）に複製して改変。あおと決めた方針: ①同じサイトの別ページ ②データは通常版と分ける（`fes_`付きの別テーブル。来場者に常時ログ実験メンバーの位置が見えないように＋集計を分けるため） ③18歳以上のまま ④開催時間中だけ使える・アンケート導線・参加人数表示を追加。テーブルは `supabase/fes_tables.sql` で既存テーブルから列・制約・RLSポリシーを読み取って複製する（定義がリポジトリに無いため決め打ちしない）。`push_subscriptions`はEdge Functionが読むので共用。開催日時とアンケートURLは`fes/index.html`先頭の`FES`設定にあり、**現在はテスト用に9/13〜12/31の仮期間・アンケートURL未設定**。ローカルでNode＋ヘッドレスChromeを使い、開始前/開催中/終了間際/終了後の画面表示を確認済み。**SQL未実行のため実データでの動作は未確認**。
- 2026-09-13: あおの指示で、学園祭まではメンバーだけでフェス版を動作確認するため**トップURLをフェス版に転送**する`vercel.json`を追加（`/`と`/index.html`→`/fes/`、一時リダイレクト）。通常版の`index.html`自体は変更していないので、**`vercel.json`を消せば通常版に戻る**。あわせて`FES.alwaysOpen`を追加（`true`の間は開催時間を無視していつでも使える）。**本番前に`alwaysOpen:false`にして`sessions`に本番日時を入れる**こと。注意: この間メンバーのデータは`fes_`テーブルに入るため、常時ログ実験（ロードマップB-1）のすれ違いデータも`fes_encounters`側に貯まる。
- 2026-09-13: あおが`supabase/fes_tables.sql`を本番Supabaseで実行。`fes_`テーブル10個すべて作成され、RLSポリシー数が通常版と一致、通常版テーブルを参照したままのポリシーも0件であることを確認済み。残りはPR作成→mainへのマージ→メンバーでの実機確認。
- 2026-09-13: 学園祭版の改良を開始（`feature/fes-map`、未コミット）。あおと決めた方針（地図は本物の地図／キャンパス外は画面をふさぐ／出店広告は運営が登録／属性一致しない人は画面上だけ隠し、データは全員分記録／閉じている間の通知は「最後にいた位置」＋「時間で全員に」）のうち、まず**地図**を実装。レーダー図をLeaflet＋OpenStreetMapに置き換え、多摩キャンパスの敷地（OSM way 597200868 を8m単位で間引き、元の形と99.64%一致）を表示。`FES.campusOnly`（**今は`false`＝どこでも使える。本番前に`true`**）でキャンパス外は画面をふさぎ位置を送らない・敷地外の人は一覧に出さない。すれ違い判定を500m→`FES.encounterRangeM`=50m（GPS実測前の仮値）にし、精度が`FES.maxAccuracyM`=50mより悪い時は判定しない。テストモードの位置を渋谷→多摩キャンパス中心に変更。ヘッドレスChromeで地図・キャンパス外画面を確認済み。
- 2026-09-13: 学園祭版の**属性**を実装（`feature/fes-map`、未コミット）。`fes_profiles.fes_attrs`（jsonb 1列。お題や選択肢がMTGで変わってもSQL再実行不要にするため）に、学部（必須・一致判定）／学科学年（任意・表示のみ）／今日の目的（必須・一致判定）／目標（任意・一致判定）／いまの状態（近くの人画面でいつでも変更・表示のみ）／出身都道府県（任意・表示のみ）／2択×3（必須・表示のみ、同じ答えは「同じ！」）を保存。選択肢は`fes/index.html`の`FA`。学科名は公式サイトで確認（スポーツ健康学部はコース）。**2択のお題は仮で、9/15のMTGで決める**。「なんとなく」「まだ考え中」は一致判定に数えない。一覧・地図・すれ違い一覧は共通点がある人だけ表示し、すれ違いの記録（`fes_encounters`）は全員分とる（共通点のない人とのすれ違いは名前も通知しない）。いいね時に何が共通していたかを`fes_likes.matched`に記録（どの共通点がマッチにつながったか後で集計するため）。通常版の趣味項目は登録画面の「任意」の折りたたみに移動。属性追加前に登録した人には初回に編集画面を出す。**`supabase/fes_attributes.sql`が未実行の間は属性なしで保存し直す**ようにしてあり、位置共有は止まらない。ヘッドレスChromeで一致判定・表示の絞り込み・画面を確認済み。
- 2026-09-13: 学園祭版の**出店の広告**を実装（`feature/fes-map`、未コミット）。`fes_booths`テーブル（`supabase/fes_booths.sql`、**運営がSQL Editorで登録**。アプリには読み取りポリシーだけ付けて書き込みを不可に）を地図にマーク表示し、アプリを開いている間に近くを通ると「近くにこんなお店があります！」カードを出す。範囲は屋外`FES.boothRangeM`=50m／屋内`FES.boothIndoorRangeM`=80m（屋内はGPSが当てにならないので距離は出さず場所の説明だけ）、同じ出店は`FES.boothCooldownMin`=30分に1回（端末のlocalStorageで管理）、位置のずれが大きい時は出さない。テストモードで試しの出店を1つ出す。ヘッドレスChromeで判定（近い順・クールダウン・屋内範囲・精度不良時）を確認済み。**宣伝してよいかは渉外担当が実行委員会に確認中**。
- 2026-09-13: 学園祭版の**閉じている間の通知**を実装（`feature/fes-map`、未コミット）。①「近くに共通点のある人がいます」: アプリを開いている端末が1分ごとに、位置更新が90秒〜`FES.nearbyPushStaleMin`=15分止まっている（＝閉じた）人のうち、閉じる直前の位置が`FES.nearbyPushRangeM`=80m以内かつ共通点がある人に、既存の`send-match-push`で通知（名前は出さず共通点だけ）。同じ人へは`fes_push_log`で全端末共通に`FES.nearbyPushCooldownMin`=30分に1回まで（記録してから送る。テーブルが無い時は連打防止のため送らない）。②時間で全員に: 運営が`fes_scheduled_pushes`に登録→pg_cronが1分ごとに新Edge Function `fes-broadcast`（`supabase/functions/fes-broadcast/index.ts`、**send-match-pushには手を入れず別関数**）を呼び、学園祭版参加者で通知許可済みの全員に送る（先に`sent_at`を入れて二重送信を防ぐ）。SQLは`supabase/fes_notifications.sql`。モックのSupabaseで①の絞り込み（距離・共通点・クールダウン・テーブル無し・精度不良）を確認、Edge Functionはesbuildで構文確認のみ（**Deno環境が無く実行は未確認**）。**学園祭後は`select cron.unschedule('fes-broadcast');`で自動実行を止める**こと。
- 2026-09-13: あおが本番Supabaseで `fes_attributes.sql`（`fes_attrs`/`matched`列の追加を確認）→`fes_booths.sql`（テーブル＋ポリシー1件を確認）→Edge Function `fes-broadcast` のダッシュボードからのデプロイ（Verify JWT はONのまま）→`fes_notifications.sql`（cronジョブ `fes-broadcast` が active=true を確認）を実行。コード側（`feature/fes-map`）は未コミット・未マージ。
- 2026-09-13: PR #13 マージ後、あおの指示で学園祭版の**地図を多摩キャンパスだけに**（`feature/fes-map-clear`、未コミット）。キャンパスの外を白く覆い（世界の四角にキャンパス形の穴をあけた多角形）、`maxBounds`でキャンパス外へ動かせない・`minZoom`でキャンパス全体より引けないようにした。キャンパス外にいる人・自分・出店のアイコンは地図に出さない（一覧は別）。地図画像は**航空写真＝国土地理院 seamlessphoto（ズーム18まで、以上はぼやけるので`maxZoom:18`）**、**イラスト＝外部の地図サービスを使わず、OSMから取ったキャンパス内の建物32・車道40・歩道80・グラウンド13・緑地11・池3の形（`fes/campus-geo.js`、約37KB）をLeafletのcanvasで描画（ズーム19まで、道の太さはズームに合わせる）**を「航空写真／イラスト」ボタンで切り替え（選択はlocalStorage、campus-geo.jsが読めない時は航空写真のまま）。国土地理院の淡色地図は拡大時に等高線と巨大な地名が出て見づらく、CARTOは「API KEY REQUIRED」の透かしが出る（APIキー必須）ため不採用。OSMから取った建物名23件（総合棟・経済学部A〜C棟など）をズーム17以上でラベル表示。地図の高さ250→300px。テストモードの人を20〜60mに置くよう変更（遠いと敷地外に出て映らなかった）。
- 2026-09-13: PR #14 をマージし、本番の `/fes/` に新しい地図と `campus-geo.js` が出ていることを確認。あわせてロードマップ（リポジトリ外）を更新、9/15 MTGの議事録の下書きを作成し、どちらもPDF化してメンバー共有用にした。体制を「アプリ開発陣（あお＝水原・しゅうすけ＝山本）／アイデア班（りく・りこ・ずな・はな）」に分け、ロードマップの担当を班の名前で書き直した。「今日はここまで」の指示で引継ぎノートを上書き（`docs/handoff-2026-09-13`）。
- 2026-09-15: **多摩キャン版**（`/tama/`）を作成（`feature/tama-edition`）。目的は学園祭の前に取っておく保険のデータで、学食にQRを貼って入ってもらい、多摩キャンパス内でマッチングする。あおと相談し「同じリンクで切り替え」でも「別リポジトリ」でもなく、学園祭版と同じく同じリポジトリの別URLにした（データの混ざり・分岐の増加・Supabaseの二重管理を避けるため）。`fes/index.html` を複製し、テーブル接頭辞を `tama_`、manifest・通知の飛び先・正式URL・localStorageのキーを `/tama/` 用に、表示名を「多摩キャン版」に変更。地図の形は `/fes/campus-geo.js` を共用（**学園祭後に fes/ を消すと多摩キャン版の地図のイラストも消える**）。テーブルは `supabase/tama_tables.sql` で **fes_ テーブルから**（属性の列・ポリシー込みで）11個複製する。出店と時間で全員に送る通知は作らない。**SQL未実行**（PR #16 https://github.com/yamamoto0728/-/pull/16 を作成済み・未マージ）。今日の目的の選択肢・2択のお題・出店の広告など学園祭向けの中身はそのままで、あおの指示待ち。**QRを貼る前に `FES.campusOnly` を `true`** にする。あわせて、あおの指示で**学園祭版・多摩キャン版とも、現在地（場所名・座標・精度・プロフィールの📍）を多摩キャンパスの中にいる時だけ表示**するよう変更（自宅の町名が出ていたため）。キャンパス外では「キャンパスの外にいます」と出し、町名を調べる Nominatim への座標送信もやめた（場所名は「多摩キャンパス」固定。すれ違い記録の場所名もキャンパス外では「この付近」になる）。Nodeで構文と中/外の表示切り替えを確認済み。
- 2026-09-15: あおの意図は「表示を隠す」ではなく「**位置の機能をキャンパス内だけで動かす**」だったので追加で変更（学園祭版・多摩キャン版とも）。`FES.campusOnly` の意味を「外では画面ごとふさぐ」から「**外では位置の機能だけ止める**」に変え、**`true` にした**（家から位置の機能を試したい時だけ `false`）。外にいる間は、位置を送らない・他の人の位置を取りに行かない・近くの人の一覧と地図に出さない/出ない・すれ違いを記録しない・「近くにいます」通知と出店の判定をしない。キャンパスから出た瞬間に `profiles.sharing=false` を保存し、最後のキャンパス内の位置が他の人の一覧・参加人数・通知に使われないようにした（共有スイッチを切った時と同じ。戻ると `publishMe` が `sharing:true` で送り直す）。**プロフィール・マッチ・チャット・コミュニティは外でも使える**（あおの確認済み）。画面全体をふさぐ `#fesOutside` は削除し、一覧に「キャンパスに入ると表示が始まります」を出す。同意画面の説明も「キャンパスの中にいる間だけ共有」に変更。メンバーが家で位置の機能（地図・すれ違いなど）を試すには、一時的に `FES.campusOnly` を `false` にする（「🧪 1台で動作確認する」は位置が取れていない時だけテスト地点をキャンパス中心にするので、家でGPSが取れていると試しの人は一覧に出るが地図には出ない）。Nodeのモックで、外に出た時に sharing:false を1回だけ保存・他の人とすれ違い判定を消す・戻ると再開することを確認済み。
- 2026-09-15: あおの指示で**トップURL（`/`・`/index.html`）の転送先を `/fes/` → `/tama/` に変更**（`vercel.json`）。学園祭版は「いったん待機」＝コードはそのままで、`/fes/` を直接開けば使える状態（`alwaysOpen:true`）。版の切り替えはアプリ内には無く、配るURLと `vercel.json` の転送先で決まる。学園祭が近づいたら転送先を `/fes/` に戻す。
- 2026-09-15: PR #16 マージ後、あおの指示で**多摩キャン版の地図を見やすく**（`feature/tama-map`、`tama/index.html` だけ。学園祭版は変更なし）。①**航空写真をやめてイラストだけ**に（`campus-geo.js` が読めない時だけ航空写真で代わりに映す）。②イラストを改善：地面・緑地・池・グラウンドの色を濃くしてふち取り、歩道にもふちを付けてベージュの地面で道筋が見えるように、建物に影を付け、**名前のある建物は青・食堂はオレンジ**で色分け、駐車場は点線。③**建物名を引いた状態から表示**：ズーム17で一括表示をやめ、名前どうしが重ならない分だけ優先度順に出す（`layoutLabels()`、優先度は食堂→主要な建物→その他。拡大するほど増え、文字も大きくなる）。④**拡大・縮小しやすく**：ズームを0.5段階に、上限を19→20に、右側に押しやすい大きさの「＋／－／🏫（キャンパス全体）」ボタン、左下の「⤢ 大きく」で地図を画面いっぱいに表示（プロフィールのシートより下）、地図の高さ300→360px。ヘッドレスChromeで、全体・1段拡大・大きく拡大・全画面のスクリーンショットを確認済み（実機は未確認）。
- 2026-09-15: あおの希望で多摩キャン版に**友達と「いまどこ」（Whoのような機能）**を追加（同じ `feature/tama-map` ブランチ、`tama/index.html` と新しい `supabase/tama_friends.sql`）。あおと決めた仕様: ①友達は**申請→承認**（プロフィールのシートに「友達申請する／承認する／申請中／友達をやめる」） ②友達に見せる範囲は**本人が選ぶ**（地図の位置も見せる／建物と教室だけ／友達にも見せない） ③建物は位置から自動、**建物に入ったら「いまどこ？」のシートで教室・ひとことを聞く＋いつでも編集** ④教室・ひとことは**建物を出るか90分で消える**（`FES.roomExpireMin`）。ナビの「招待」タブを**「友達」タブ**に置き換え（自分のいまどこ・公開範囲・届いた申請・友達の一覧・送った申請、下に招待QR）。申請と承認はプッシュ通知（既存の `send-match-push`）。「地図の位置も見せる」友達は近くの人の地図に緑のふち＋名前で出る（共通点がなくても）。データは `tama_friends`（申請の行、status pending/accepted）と `tama_presence`（1人1行）で、**tama_presence は RLS で本人と承認済みの友達だけが読める（hidden の人は友達からも読めない）**。建物の判定は `campus-geo.js` の建物・グラウンドの形に `CAMPUS_LABELS` の名前を付けたもの（21か所、門は除外。名前の点が形の外にある経済学部B棟・現代福祉学部棟・スポーツ健康学部棟は60m以内の近い形に付ける）。GPSのふらつき対策で、入る判定は20秒、出る判定は3分同じ状態が続いた時（`FES.placeEnterSec/placeLeaveSec`）、精度50mより悪い位置は無視、壁ぎわは8mずらした4点も調べる。閉じている間は判定できない（ブラウザの制約）ので、友達の画面では「◯分前」を出し、最後の保存から90分たったら出さない。**あおが本番Supabaseで `tama_friends.sql` を実行済み（ポリシー数 tama_friends=4 / tama_presence=4 を確認）。PR #17 https://github.com/yamamoto0728/-/pull/17 でマージ済み**（地図の改善と一緒のPR）。「近くの人」への表示は従来どおり「位置情報を共有」で決まり、「友達にも見せない」にしても近くの人には出る（画面に注記あり）。Nodeのモックで建物の名前付けと入った・出たの判定を確認済み。
- 2026-09-15: PR #17 のマージ後、本番の `/tama/` に地図の改善と友達タブ・建物判定が出ていることを確認（トップURLも `/tama/` に転送）。「今日はここまで」の指示で引継ぎノートを上書き（`docs/handoff-2026-09-15`）。
- 2026-09-22: あおの指示で、**通知の許可をアプリを開いた時点で自動で聞く**ように変更（`feature/auto-push-permission`、3つの版すべて）。これまでは設定タブの通知スイッチを自分で押してもらう必要があり、そこまで辿り着かない人は通知が付かないままだった。起動から0.6秒後に `autoAskPush()` を呼び、説明の画面 `#pushGate`（🔔＋「通知をオンにしてください」）を出してから `Notification.requestPermission()` を実行する。**Android/PCはこれで触らずに許可画面が出る**。**iPhoneはWebKitの制約で「画面を触った直後」でないと許可画面を出せない**（触らずに呼ぶと `NotAllowedError` で即失敗する）ので、失敗したら説明の画面をそのまま残し、**画面のどこを触っても許可画面が出る**ようにした（実質タップ1回。完全な自動は技術的に不可能）。あわせて、許可済みなのに購読が消えている端末を起動時に黙って登録し直す `syncPushSubscription()` を追加（通知を「常にオン」に保つため。ログイン・新規登録・アプリに戻った時に実行）。設定のスイッチで自分でオフにした人には聞き直さない（localStorage `pushOff`）。「あとで」は4秒たってから出す。一度「拒否」されると端末の設定からしか戻せないので、設定タブの説明文を状態に応じて出し分ける `pushHintText()` も追加。ヘッドレスChromeで、Android想定（触らずに許可画面）・iPhone想定（失敗→タップで許可画面）・すでに許可済み／拒否済み（説明の画面を出さない）を3つの版すべてで確認済み。**実機は未確認**。PR #19 でマージ済み。
- 2026-09-22: あおの指示で**多摩キャン版を全機能解放**にした（`feature/tama-unlock-all`、`tama/index.html` だけ）。実証実験では課金の検証をしないため、設定画面の課金デモ（💬メッセージ機能・♥今日のいいね・🎯いいねの理由・✨今後の有料機能・「課金状態をリセット（デモ用）」）と、`PRICE`・`billing`・各 Paywall シート・`buy*` 関数をすべて撤去。**いいねは無制限**、**メッセージは最初から送り放題**、**いいねの理由は最初から見える**、**コミュニティの作成も無料**（¥500の確認ダイアログを撤去）。**マッチの24時間期限はあおの判断で残した**ので、有料の「期限をなくす」（`showExtendPaywall`/`buyExtend`）と、それを支える `unlockedMatches`・`loadMatchUnlocks`・`isUnlocked`・`tama_match_unlocks` の読み書きだけを撤去（`supabase/match_unlocks.sql` はリポジトリに無く、読み込み警告が毎回出ていた）。利用規約の「有料機能を模した画面が含まれます」と使い方ガイドの文言も直し、使わなくなったCSS（`.ttlbar.keep`・`.extendbtn`・`.keeptag`・`.whylike.locked`）も削除。`profiles.billing` 列はDBに残したまま、読み書きをやめただけ。ヘッドレスChrome＋偽のsupabase-jsで、いいね6回で課金シートが出ないこと・チャットが送れること・設定画面に「課金／有料／解禁／¥」が残っていないことを確認済み。
- 2026-09-22: あおの指示で**端末ごとにユーザー情報が記憶できているかを再確認**し、**実害のある不具合を1つ見つけて直した**（3つの版すべて）。記憶の仕組みは「Supabaseの匿名ログインの情報が端末のlocalStorageに残る → `initBackend()` がそれを読んで `myId` を復元 → `tryResumeSession()` が `profiles` から読み戻す」。同じ端末・同じブラウザなら正しく復元されることを確認。**不具合**: `initBackend()` が `sb.auth.getUser()` だけを見ていたが、これは毎回Supabaseに問い合わせに行くため、**電波が悪い・Supabaseが一瞬落ちた状態で起動すると user が null で返り、そのまま `signInAnonymously()` が走って別人の新規ユーザーになっていた**（プロフィール・マッチ・すれ違いの履歴が本人から見えなくなる）。先に通信しない `getSession()` を見るようにして修正。偽のsupabase-jsを使ったヘッドレスChromeで、修正前は「通信失敗→別IDになる」・修正後は「通信失敗→同じIDでプロフィールに復帰」することを確認済み。**⚠️ iPhoneでSafariのタブとホーム画面アプリはストレージが別**の可能性が高く、その場合「QRで開いて登録→ホーム画面に追加→開く」で別人になる。実機確認が必要。
- 2026-09-22: あおの指示で**多摩キャン版の属性を学園祭向けから普段のキャンパス向けに差し替え**（同じ `feature/tama-unlock-all`、`tama/index.html` だけ）。選択肢はあおがボタンで決定。①**学部は多摩の4学部に絞る**（経済／社会／現代福祉／スポーツ健康＋大学院／他大学の学生／その他。市ヶ谷・小金井・社会人・卒業生の行は削除）②**学年は今までどおり任意・見せるだけ**（共通点の判定は「学部・いま探していること・目標」の3つのまま）③「今日の目的」→**「いま探していること」**（一緒にごはん／授業・履修の情報交換／一緒に勉強・課題／友達をつくる／サークル・団体の仲間／ひまつぶしに話す／いまはひとりがいい。最後の1つは `weak` で一致判定に数えない）④目標に「ゼミ・研究」を追加し「資格」→「資格・語学」「就活」→「就活・インターン」⑤いまの状態＝一緒にごはん行ける／ひまです／勉強中／授業・移動中／これから帰る ⑥**2択3問を廃止し、「今季絶対取りたい科目」の自由記述1つ（必須・見せるだけ）に置き換え**。同じ科目名なら相手のプロフィールに「同じ！」が出る（`normCourse` でNFKC正規化＋空白除去＋小文字化して比較するので「ﾏｰｹﾃｨﾝｸﾞ論」や前後の空白でも一致する）。NGワードチェックを科目名にもかけ、いいねの理由の候補にも「◯◯を狙ってる」を追加。`tama_likes.matched` は `{common:[...],same:[...]}` から `{common:[...],sameCourse:'科目名'|null}` に変更。**DBの列は変えていない**（`fes_attrs` jsonb の中身が変わるだけ、`purposes` のキー名も据え置き）ので**SQL実行は不要**。古い値が残っている人は `attrStateFrom` がFAに無い値を落とすので必須が空になり、起動時に編集画面が自動で開いて選び直してもらえる（ヘッドレスChromeで確認済み）。使わなくなったCSS（`.qrow`・`.qab`）も削除。PR #20 でマージ済み。
- 2026-09-22: あおの指示で、**「いま探していること」の入力欄をマイページ（登録画面・プロフィール編集）から友達タブの「いまどこ？」シートに移した**（`feature/tama-purposes-in-presence`、`tama/index.html` だけ）。**データの置き場所は `fes_attrs.purposes` のまま**にしてある（あおの判断）。`tama_presence` に移すと承認済みの友達しか読めず（RLS）、共通点の判定軸が「学部・目標（任意）」の2つだけになって「共通点のある人が0人」が増えるため。つまり建物・教室・ひとことは友達だけ・90分で消えるのに対し、**「いま探していること」は全員に見えて消えず、近くの人一覧の絞り込みに使われる**。シート内で区切り線と注記で違いを示している。登録時には聞かなくなったので**必須ではなく任意**になった（`attrMissing` から外した）。見つけてもらう導線は、友達タブの「いまの自分」に現在の選択を表示＋「✏️ いまどこ・探していることを編集」ボタン、近くの人が0人のときの空表示のボタンを「いま探していることを選ぶ」に変更、の2つ。プロフィール編集の保存（`saveEdit`）は `purposes` を触らず、いまの値をそのまま引き継ぐ。
  **あわせて保存の不具合を1つ修正**: `publishMe()` は位置が取れていない時・キャンパスの外・共有オフだと何も送らないので、それだけに任せると「保存しました」と出たのに消えている状態になっていた（ヘッドレスChromeで再現。DBの `purposes` が空のままだった）。属性だけを書き戻す `saveMyAttrs()` を足し、`savePlaceEdit()` と `setStatus()`（いまの状態も同じ不具合があった）の両方から呼ぶようにした。
- 2026-09-24: あおが多摩キャンで実機を触ったレビューを反映（`feature/tama-review-0924`、`tama/index.html` だけ、未コミット）。①**「いまどこ？」がアプリを閉じると消える不具合を修正**：起動時に自分の `tama_presence` を読み戻しておらず（読んでいたのは公開範囲だけ）、空の状態から始めて建物に入った判定で教室・ひとことを空のまま保存していた。`restoreMyPresence()` を追加し、読み戻すまで `detectPlace()` を止める。建物は最後の保存から `roomExpireMin` 分以内なら引き継ぎ、同じ建物ではシートを出し直さない。あわせて、精度の悪い位置で「キャンパスの外に出た」と判定して消さないよう、精度の確認を先にした。②**アプリを閉じた人も、その日（日本時間）のうちは最後にいた位置に地図・一覧で表示**（あおの判断。`pollUsers` を「直近1分」→「今日0時以降」に。1分以上更新がない人はアイコンを薄くし一覧に「◯分前」）。**閉じた人とのすれ違いも数える**（あおの判断）。キャンパスから出た人は従来どおり `sharing:false` で消える。「いま◯人が参加中」は直近1分のまま。③自分のアイコン 34→30px（絵文字18→16px）。④「近くの人」は地図の下の一覧だけがスクロールしていてスマホでは狭かったので、見出しより下を画面ごとスクロールするようにした。偽のsupabase-js＋ヘッドレスChrome（DevToolsプロトコルで待ってから撮影）で確認済み。
- 2026-09-24（続き）: あおから多摩キャンの時間割（平日 1限9:20-11:00／2限11:10-12:50／3限13:40-15:20／4限15:30-17:10／5限17:20-19:00、6・7限なし）をもらい、あおがボタンで決めた仕様で実装（同じブランチ、未コミット）。①**「いまどこ？」の教室・ひとことは授業の終わりに消える**（`FES.periods`・`roomExpiryAt()`。授業中→その時限の終わり、10分休み・昼休み・1限の前→次の時限の終わり、5限の後・土日→90分）。友達の画面に出す期限も「最後の保存から90分か、消える時刻の遅い方」（`presenceStale`）に。②**時間割（履修）**：友達タブの「いまの自分」から、月〜金×1〜5限のマスを押して登録、授業名は任意。**本人しか読めない**（`tama_timetable` のRLS）。同じ曜日・時限に同じ授業名（表記ゆれをそろえて比較）を入れた人だけを関数 `tama_same_course_peers()`（security definer）が返し、「同じ授業「◯◯」」として**共通点に数える**（近くの人に出る・プロフィールに「📚 同じ授業 木2 ◯◯」）。③**通知**：新しい Edge Function `tama-remind` を pg_cron が平日に呼ぶ（`supabase/tama_remind.sql`）。**授業開始5分後（9:25/11:15/13:45/15:35/17:25）にその時限に授業がある人だけ**「📍◯限が始まりました」、**昼休み12:55に参加者全員**「🍛 昼休み、ひまな人いる？」。時間割を入れていない人に届くのは昼休みの1回だけ。10分休みには送らない。**あおの作業待ち：`tama_timetable.sql` 実行 → `tama-remind` デプロイ → `tama_remind.sql` 実行**（この順）。休講日・長期休みは `tama_remind.sql` 末尾のコマンドで止める。祝日は自動では止まらない。偽のsupabase-js＋ヘッドレスChromeで、消える時刻8パターン・同じ授業の共通点・時間割の画面を確認済み。Edge Function は Deno が無く未実行。**時間割も研究データになるので、利用規約・同意画面への追記をアイデア班（データ）と要相談。**
- 2026-09-24（続き）: あおの「友達と交換するだけのアプリになる、偶然のマッチングが足りない」という相談から、**今日のお題**を追加（同じブランチ、未コミット、`tama/index.html`）。偶然らしさは「同じ答えの人が、今たまたま近くにいた」という時間と場所の重なりから出す、という方針。「近くの人」の地図の上にカードを出し、1日1問（日本時間の日付で `DAILY_QS` を順番に出す。一周したら最初に戻る＝**毎日の作業ゼロ**）。**選択式（2〜4択）と自由記述（好きなアーティストは？など）を混ぜる**（あおの案。選択式は一致しやすく話しかける口実、自由記述はめったに一致しないぶん一致した時の価値が大きい）。自由記述は全角半角・大文字小文字・ひらがなカタカナ・空白記号をそろえて比べる（漢字とかなの違いは一致しない）。**答えはその日1回だけで変更不可**、**同じ答えの人を共通点に数える**（学部などが違ってもその日だけ近くの人に出る）、カードに「同じ答えの人 近くに◯人・今日すれ違った人に◯人・キャンパスに◯人」、相手のプロフィールに今日の答え（あおの判断）。答えは `fes_attrs.daily={d,q,a}` に入れるので**SQL不要**。プロフィール編集の保存で消えないよう `saveEdit` で引き継ぐ。**お題30問は仮（Claude作成）。アイデア班が作ったら差し替え、足す時は末尾に**。昼休みの通知（`tama-remind`）の文面を「🎲 今日のお題、もう答えた？」に変更。ヘッドレスChromeで確認済み。
- 2026-09-24（続き）: あおの「流行ったら何人まで記録できる？」から**サーバーの容量を見積もり**。**Supabaseは無料プラン**（あお確認）。保存容量（500MB）・匿名ユーザー数（月5万）は数万人でも余裕で、**限界は通信量（無料は月5GB）**。6秒ごとに全員分のプロフィールを取り直していたので通信量は「開いている人数×キャンパスにいる人数」で増え、しかも同日の「閉じた人も今日いっぱい地図に残す」変更で受け取る人数が増えていた。あおの判断で**同じブランチで対策**：①他の人は手元（`userCache`）に持ち、前回から更新があった人だけ取る（`updated_at` が書いた端末の時計なので8秒さかのぼる。全員分の取り直しは10分に1回と日付が変わった時）②取る列を絞る（`USER_COLS`）③自分の位置は5m動いた時か45秒ごとだけ送る（`publishMeIfNeeded`。GPSの更新ごと・6秒ごとに送っていた）④共有を止める・キャンパスを出る時の `sharing:false` に `updated_at` も入れる（他の端末に伝わるように）⑤「閉じた人」の基準を1分→90秒（`CLOSED_AFTER_MS`。「いま◯人が参加中」も同じ）⑥「近くにいます」通知の候補は別に問い合わせず手元の others から選ぶ⑦マッチの確認は18秒ごと。偽のsupabase-jsで、止まっている間の送信が20秒で1回・共有を止めた人が消える・古い人を取り直さないことを確認。**概算で同時100人・今日来た人300人のとき 約18GB/時 → 約1.5GB/時**（1人分1KB、圧縮なしの見積もり）。**本当に流行ったら無料プランでは足りない**。**あおの決定：流行ったらPro（月$25・250GB）にする**（ダッシュボードの Usage で通信量を見て判断。プランの管理はしゅうすけさん）。それでも足りなければ、位置だけの軽いテーブルに分ける（ハートビートでプロフィール全体を配らない）。
- 2026-09-24（続き）: PR #22 でマージ済み・本番の `/tama/` に反映を確認。**あおが本番Supabaseで `tama_timetable.sql` 実行 → Edge Function `tama-remind` をダッシュボードからデプロイ → `tama_remind.sql` 実行まで完了**（平日の定時の通知が動き始める）。実機での通知の受信は未確認。
- 2026-09-24（続き）: あおの指示で**今日のお題を30問→150問に**（種類ごとに5倍：2択20・3択30・4択50・自由記述50。約5か月で一周）。`feature/tama-daily-150`、未コミット。内容は任されたので、心理学の研究を調べてから作成（ささいな一致でも好感が上がる／似ていることが効くのは初対面の段階／めずらしい好みの一致ほど惹かれる→自由記述は具体的に／軽い「苦手」の一致は距離を縮める／同じ瞬間の体験の共有／初対面の話題は音楽が最多／少し踏み込んだ質問の方が会話が楽しい。出典は `DAILY_QS` の上のコメント）。恋愛・政治・宗教・容姿・家族・収入・健康、住む場所・バイト先など本人が特定される質問は入れていない。既存30問はそのまま前に残し、今日のお題が変わらないよう末尾に4種類が混ざる順番で追加（追加分の最初は9/30の「最近リピートしてる曲は？」）。ヘッドレスChromeで件数・重複なし・今日のお題が変わらないことを確認済み。
