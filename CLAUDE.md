# プロジェクト概要

**「ちかく」** — 位置情報ベースの「すれ違い」機能に特化した、恋愛に縛られないマッチングアプリ。

- 現状は `index.html` 1ファイルで完結したプロトタイプ（HTML/CSS/JS、ビルド不要）
- バックエンドは Supabase（匿名認証でユーザーIDを発行、URL/anonキーはコード内に直書き）
- 主な機能: オンボーディング（絵文字アバター・タグ・興味関心選択）、現在地取得と近くのユーザー一覧・地図表示、NGワードフィルタによる不適切投稿の簡易ブロック
- リポジトリ: https://github.com/yamamoto0728/-

## チーム構成

- **yamamoto0728** — リポジトリオーナー
- **あお（blue.aochan03@gmail.com）** — 開発メンバー（Claude Codeと一緒に作業）

# 引継ぎノート（最新セッションの状態）

新しいセッションでこのリポジトリを開いたら、まずこのセクションを読む。過去の詳細な履歴は下の「進捗ログ」を参照。

## 今回やったこと（2026-08-02）— プッシュ通知の完了 ＋ コミュニティ機能の土台づくり

**このセッションの成果はすべて `main` にマージ済み・Supabase側の設定も適用済み。コードとDBは揃っている状態。残っているのは実機での動作確認だけ。**

### 1. プッシュ通知のRLS問題が解決（決着）

前回特定した根本原因（`push_subscriptions`にSELECTポリシーが無く、supabase-jsの`.upsert()`がRLSで403になる）について、あおがSupabase SQL Editorで`create policy "select own subscription" ... for select using (auth.uid() = id)`を実行し、**通知トグルONが成功することを実機で確認**。数セッション追っていた問題はこれで決着。

### 2. いいね通知の追加（PR #8、マージ済み）

- `notifyMatchPush(uid)`を汎用の`notifyPush(uid,title,body)`に整理
- **片想いいいね（マッチ未成立）の受け手にも通知**を送るようにした。**送信者名は伏せる設計**（UI上も片想いいいねの送信者は表示していないため、情報量を揃えた）
- マッチ時の通知は名前を出す（相互いいね成立後なので）

### 3. 差別化の方向性を議論（重要・次の設計の前提）

すれ違いはHappnが、「恋だけじゃない」はBumble/Yenta/Timeleftが既にやっており、**現状の2本柱はどちらも埋まっている**という認識で一致。

ちかくの独自資産は `encounters` が持つ **count（反復回数）＋ last_place（場所）**。これは「偶然すれ違った」ではなく **「生活圏とリズムが同じ」の証明**になる。Happnが「すれ違った（過去・偶然）」を売って頭打ちになったのに対し、ちかくは **「また会える（未来・確実性）」** を売れる。会うまでの摩擦こそが恋愛・友達・ビジネス共通の最大のボトルネックで、そこだけが構造的に消せるため。

将来的な本命は「**場所を主役にしたコミュニティ**（その場所に実際に居る人しか入れない部屋）」＋ **B2B（店が送客の対価を払う）**。日本の消費者向けマッチングアプリのB2C課金は上位3社以外CACで死ぬが、「送客」は店が実際に金を払っている領域のため。

### 4. コミュニティ機能の土台（PR #9 → #10 でマージ済み）

機能を足す前に、コードを読んで見つかった構造的な穴を塞いだ:

1. **`room_members` テーブルを新設**（`supabase/room_members.sql`、**本番で実行済み**）。今まで部屋に「所属」の概念が無く、メンバー数は「直近200件に投稿した人」というROM専を無視した偽の数字だった
2. **参加/退出ボタン**。未参加でも閲覧はでき、投稿だけ制限。部屋を作った本人は自動参加
3. **N+1クエリを解消**。部屋ごとの問い合わせを rooms/所属/直近メッセージの**3クエリ**に集約（ホーム画面を開くたびに部屋数ぶんクエリが飛んでいた）
4. **未読バッジ**。`last_read_at`と投稿時刻の比較で未読数を出し、コミュタブに赤バッジ（`dotComm`）。未読のある部屋が一覧の上に来るよう並び替え
5. **コミュニティ投稿のプッシュ通知**。既存Edge Function `send-match-push` は**一切変更せず**流用（動いているマッチ通知を壊さないため）。直近5分以内に部屋を見ていた人には送らないので通知連打にならない
6. **NGワードフィルタをメッセージに適用**。`ngCheck()`はプロフィール名/自己紹介にしか効いておらず、**1対1チャットもコミュニティ投稿も素通りだった**。部屋名・ひとことにも適用
7. **残っていたRLSデバッグコードを撤去**。`enablePush()`に`token.sub`のJWTデコードと**`profiles`への診断書き込みが通知ONのたびに毎回実行される状態**で残っていた（PR #8では`notifyMatchPush`側しか消せていなかった）。`alert()`は全廃し`toast()`＋`console.error`に統一
8. 既存バグ修正: `openRoom()`が`r.id===id`で比較しており、`rooms.id`が数値だとonclick経由の文字列と一致せず部屋名がフォールバックしていた

**設計上の判断メモ**:
- `rooms.id`の型（uuid/bigint）がリポジトリに記録されていないため、SQLは既存の`rooms.id`から型を読み取って合わせる書き方にした。決め打ちして型不一致で失敗すると、また原因調査から始まるため
- 同じ理由で、クライアント側も部屋IDは常に`String()`で正規化して比較する（`rid()`ヘルパー）
- `room_members`のRLSには**最初からSELECTポリシーを入れた**。`push_subscriptions`でこれが無くて403になり数セッション溶かしたため
- SQL実行前にコードがデプロイされても部屋一覧が壊れないようにし、原因がコンソールに出るよう警告を仕込んである（`[rooms]`で始まる行）

## ブロッカー・待ち状態

- **ブロッカーなし。** コード（main）・DB（Supabase）とも反映済みで、あとは動くかどうかの確認だけ
- 誰の返事待ちでもない

## 次回やること

### まず最初に: 実機での動作確認（未実施）

コミュニティ機能は**コードもDBも入っているが、一度も実機で動かしていない**。次回はまずここから:

1. コミュ画面 → 部屋を開く → 右上に「参加」ボタンが出るか
2. 未参加だと投稿欄が押せず「参加すると投稿できます」と出るか
3. 「参加」を押すと「参加中」に変わり、投稿できるようになるか
4. 一覧に戻ると「参加中」タグが付き、人数が実際のメンバー数になるか
5. **別端末**から同じ部屋に投稿 → 通知が届き、コミュタブに赤バッジが付くか
   - ※受け取る側は**部屋を閉じた状態**で試すこと。開いていると「今見ている人」と判定されて通知は飛ばない仕様（5分ルール）

うまく動かない場合、ブラウザのコンソールで `[rooms]` `[push]` で始まる行を見ると原因が絞れる。

### そのあと: 期限付きコミュニティ（議論済み・設計まで完了）

今の部屋は**永続・全国共通**なので必然的に過疎る。人が少ない初期に、誰もいない常設部屋が並ぶのは一番きつい。そこで**マッチと同じく部屋に期限を持たせる**（「今夜このへんで飲みたい人」「今週末カメラ持って歩く人」のような数日で消える部屋）。

効く理由:
- **少人数で成立する**（3人でも「今夜」なら成立。常設部屋は30人いないと寂しい）
- **墓場が残らない**（過疎部屋が自動で消え、リストが常に生きて見える）
- **課金の筋が通る**（「¥500で永続の箱を買う」より「今夜の集まりを立てる」の方が払う理由が明確で、失敗しても消えるので損失感が小さい）
- **恋だけじゃないが自然に成立**（目的が部屋ごとに書かれるので宣言不要）

### その先: 場所ゲート

「その場所に実際に居た人だけ入れる部屋」。差別化としては最強だが、`reverseGeocode()`が町名レベル（zoom=16、「渋谷 道玄坂」止まり）までしか出ないため、**店舗単位の粒度をどう取るかの技術検証が先**。ここが無理だと構想全体が成立しないので、着手前に必ず検証する。

## 未解決の疑問点

- **部屋作成¥500の課金設計は手を付けていない**。空部屋のリスクを作成者が全部背負う設計で、乱立→全部過疎の失敗コースに乗りやすい。ただし課金は山本さんも関わる商品判断なので、独断で変えずに残した（**要相談**）
- いいね通知で送信者名を伏せる設計にしたが、あおの意図と合っているか未確認
- マッチ成立→Edge Function経由で実際にスマホに通知が届くエンドツーエンド確認は**依然未実施**（通知の登録が成功したところまでしか確認できていない）
- コミュニティ通知は宛先ごとにEdge Functionを1回ずつ呼ぶ実装。人数に比例して呼び出しが増えるので、規模が大きくなったらEdge Function側で配列を受け取れる拡張が必要（今回は動いているマッチ通知を壊さないことを優先して見送り）

# リポジトリの扱い方

このフォルダ（`yamamoto0728-original/`）は GitHub 上の原本と `.git` でつながった実クローン。**`main` ブランチを直接編集しない**。作業は必ずブランチを切って行う。

```
git checkout -b feature/○○
```

複数メンバーが同時に作業しても、ブランチを分けていればお互いの変更を壊さない。他メンバーが先に `main` を更新していた場合は作業ブランチ側で追従する:

```
git fetch origin
git rebase origin/main   # 衝突した場合だけ手動で解消
```

# 「マージして」と指示されたときのClaudeの役割

ユーザーから「マージして」と指示されたら、Claudeは以下を行う（**PR作成まで**。実際のmainへの取り込みはユーザー/チームが手動で行う）:

1. `git add` → `git commit`（変更内容を記録、メッセージは変更内容から作成）
2. `git push origin <作業ブランチ名>`
3. `gh pr create --base main`（Pull Requestを自動作成）
4. 作成したPRのURLを報告して完了

**Claudeはここで止まる。mainへの自動マージ・強制的な衝突解決は行わない。**

## ⚠️ PRのbaseは必ず `main` にする（2026-08-02の失敗から）

**未マージのPRの上に別のPRを積む（stacked PR）ことはしない。** 一度これをやって事故が起きた:

- PR #9 の base を、まだマージされていない `feature/like-push-notify` に設定した
- 先に PR #8 が `main` にマージされ、そのあと PR #9 がマージされた
- その結果 **PR #9 の中身は `feature/like-push-notify` に入っただけで `main` に届かず**、GitHub上は「Merged」と表示されるのに本番には反映されない状態になった
- 回収のために PR #10 を追加で作る羽目になった

前のPRの成果物に依存する作業をする場合でも、**baseは`main`のままにして、前のPRがマージされてから `git rebase origin/main` で追従する**。

## そのあとユーザーがやること

1. 提示されたPRのURLを開き、差分を確認する
2. 必要ならチームメンバーにレビューを依頼する
3. 問題なければGitHub上で「Merge pull request」を押す（これでようやく`main`＝原本に反映される）
4. コンフリクトが出ていたら、その内容をClaudeに伝えて一緒に解消する

## 前提条件

PR作成には `gh` CLI のログインが必要。**2026-08-02時点で認証済み（アカウント: blueaochan03-spec）で、Claudeが`gh pr create`まで実行できることを確認済み**。過去の引継ぎに「`gh`がこの環境に無いのでPR作成はあおが手動」と書かれていたが、それは解消されている。

## あおに操作をお願いするときの注意

**SQLやコマンドを実行してもらうときは、ファイルパスではなく中身をそのまま貼り付けて渡すこと。** 「`supabase/room_members.sql` を実行して」と伝えたところ、SQL Editorにファイルパスの文字列が貼られて `syntax error at or near "supabase"` になった実例がある。コピペできる完全な形で提示する。

# 「今日はここまで」と指示されたときのClaudeの役割

ユーザーが「今日はここまで」「一旦ここで終わり」など作業終了の合図を出したら、Claudeはこのファイル冒頭の「引継ぎノート」セクションを**上書き**して、次回セッションが読むだけで迷わず再開できる状態にする。

引継ぎノートに書く内容:

1. 今回のセッションでやったこと（要約でよい、詳細は進捗ログ参照でOK）
2. 今ブロックされていること・誰の返事/操作待ちか（例: 「山本さんのSupabase招待待ち」のように、待っている相手と対象を具体的に）
3. 次回再開したらまず何をするか
4. 未解決の疑問点・判断が必要なこと

進捗ログ（下のセクション）とは違い、引継ぎノートは追記ではなく**常に最新状態だけを残す**。これにより、次のセッションが長い進捗ログを遡って推測する必要がなくなる（今回、「山本さんに権限をもらう」がどのサイトの話か特定できず聞き直しになったのが再発防止のきっかけ）。

# 進捗ログ

ユーザーが指示するたびに、日付＋一言ログをこの下に追記していく（履歴として残す。上書きしない）。

- 2026-07-15: プロジェクトの複製・ブランチ運用・「マージして」で動く自動PR作成の仕組みを構築。CLAUDE.mdを作成し、次にこのリポジトリを開くClaudeが仕組みとプロジェクト概要を把握できるようにした。
- 2026-07-15: `feature/matching-tweak`ブランチでCLAUDE.mdをcommit→push→PR化（PR #1, https://github.com/yamamoto0728/-/pull/1 ）。ユーザーはこのリポジトリにwrite権限あり、マージボタンは押せることを確認済み。PRはまだ未マージ。
- 2026-07-15: `.claude/settings.json`を新規作成。開発系操作（ファイル編集・npm/pipインストール・lint/test・gitの読み取り系とadd/commit・ghの読み取り系）は自動承認、削除・push・PR作成/マージ・DB破壊系操作は`ask`のまま残す方針で設定。「アプリを起動してブラウザで確認する」操作（runスキル、ローカルサーバー起動）も自動承認に追加。ただし「Bashコマンド全体を無条件で自動承認」は、削除・push・DB操作などのask設定を素通りしてしまうリスクがあるため見送った。設定変更後もセッション内でプロンプトが出続けたため、IDE（Cursor）再起動で設定を読み直す必要がある可能性が高い。
- 2026-07-15: 「リンクに入った人の情報を記憶する」機能を実装。既存の匿名認証（supabase-jsのデフォルトlocalStorage永続化）自体は同一ブラウザなら`myId`を再利用できていたが、`profiles`テーブルから過去のプロフィールを読み戻す処理がなく、毎回オンボーディング画面が出ていたのが原因。`tryResumeSession()`を追加し、boot時に`myId`で`profiles`を検索、既存プロフィールがあればオンボーディングをスキップして`near`画面に直行するようにした（index.html）。制約として、匿名認証はブラウザのlocalStorageに紐づくため「同一端末・同一ブラウザ」でのみ記憶が有効（別端末では非対応、ユーザー確認済みでこの範囲でOKとの合意）。この環境にNode/Python/ブラウザ自動化ツールが無く実機でのブラウザ動作確認はできておらず、コードレビューのみで実装。ユーザー側での手動確認待ち。
- 2026-07-15: マッチ時にスマホへプッシュ通知を送る機能を追加（PWA化）。Web Push API + manifest.json/sw.jsでPWA化し、マッチ確定時にSupabase Edge Function `send-match-push` を呼んで購読済み端末に通知する仕組み（`supabase/functions/send-match-push/index.ts`, `supabase/push_subscriptions.sql`）。Supabase側（テーブル・Edge Function・VAPID鍵）は山本さんがダッシュボードで設定済み。その後、通知が実際に届いているか怪しかったため、`notifyMatchPush`の呼び出し結果をtoast/alertで可視化するデバッグ表示を追加（原因調査中、未解決）。
- 2026-07-16: プッシュ通知が届かない原因調査を進めるには、あお自身がSupabaseダッシュボード（プロジェクト参照: `rosgvnxqcuyenlipakck`）でEdge Functionsのログやsecretsを見られる権限が必要と判明。山本さんにSupabase組織のメンバー招待（Developerロール、blue.aochan03@gmail.com宛）を依頼することにした。招待待ち、権限が付与され次第デバッグ再開。
- 2026-07-16: 招待が通り、あおが初めてSupabase SQL Editor/Edge Functionsを操作。Edge Function `send-match-push`とVAPID Secretsが実際には未設定だったことが判明し、両方セットアップし直した（PR #3, https://github.com/yamamoto0728/-/pull/3 、マージ済み）。それでも通知が届かず調査を継続、`push_subscriptions`へのinsertがRLSポリシー違反で失敗していることが判明（PR #4, #5, https://github.com/yamamoto0728/-/pull/4 , https://github.com/yamamoto0728/-/pull/5 、マージ済み）。`myId`と`auth.uid()`は一致・ポリシー定義自体も正しいことを確認済みだが、根本原因は未特定のまま時間切れ。詳細は上の「引継ぎノート」参照。
- 2026-07-24: RLSエラーの根本原因を特定。SQL Editorでの検証（ポリシー同一・トリガー無し・なりすまし`auth.uid()`でのINSERT成功）でDB側は正常と確認し、アプリ側にデバッグ追加（`feature/push-debug3`、マージ済み）。結果`sub==myId? true`・`profiles書込テスト=OK`なのにpushだけ403 → **push_subscriptionsにSELECTポリシーが無く、PostgREST経由の`.upsert()`がRLSで弾かれていた**のが原因と判明。修正はSELECTポリシー追加（`create policy ... for select using (auth.uid()=id)`）。あおの実機テストでの最終確認待ちで中断。詳細は上の「引継ぎノート」参照。
- 2026-08-02: PR #9 を base=`feature/like-push-notify` の stacked PR にしたことで事故が発生。PR #8 が先に main にマージされた後に #9 がマージされたため、**#9 の中身が main に届かず feature ブランチに取り残された**（GitHub上は「Merged」表示なのに本番未反映）。PR #10 で回収し main への反映を確認。再発防止として「PRのbaseは必ずmain、依存があっても後からrebaseで追従する」をCLAUDE.mdの運用ルールに明記。あわせて、`gh` CLIは認証済みでClaudeがPR作成まで実行できること（過去の引継ぎの記述が古かった）と、あおにSQL実行を頼むときはファイルパスではなく中身を貼って渡すこと（パスをSQL Editorに貼られてエラーになった実例あり）も追記。
- 2026-08-02: コミュニティ機能の土台を整備（`feature/community-membership`）。差別化の方向性を議論し、すれ違い＝Happn・恋だけじゃない＝Bumble/Yentaで既出と確認した上で、ちかくの独自資産は`encounters`の「反復回数＋場所」＝生活圏の一致であり、売るべきは「また会える（未来）」だと結論。その第一歩としてコミュニティを深めることにし、`room_members`テーブル新設・参加/退出・未読バッジ・コミュ投稿のプッシュ通知・N+1クエリ解消・メッセージへのNGワードフィルタ適用を実装。あわせて`enablePush()`に残っていたRLSデバッグコード（JWTデコードと毎回走る`profiles`診断書き込み）を撤去し、`alert()`を全廃。あおのSQL実行待ち。詳細は上の「引継ぎノート」参照。
- 2026-08-02: あおがSupabase SQL EditorでSELECTポリシーを追加し、通知トグルONが成功することを実機確認（RLS問題は解決確定）。mainが山本さんの新機能（マッチ24h無効化・地図縮小・コミュ情報表示）で進んでいたため取り込んだ上で、`feature/like-push-notify`ブランチを作成。`supabase/push_subscriptions.sql`にSELECTポリシーを記録として追記、デバッグ用alert表示を撤去して`notifyMatchPush`を汎用の`notifyPush(uid,title,body)`に整理、**片想いいいね時にも相手へプッシュ通知を送る新機能**を追加（送信者名は伏せる設計）。あおの実機確認・マージ指示待ち。
- 2026-09-13: 学園祭の実証実験用に**学園祭版**を作成（`feature/fes-edition`、未コミット）。通常版（`index.html`）は一切変更せず、`fes/index.html`（`https://chikaku-hosei.vercel.app/fes/` で配信する想定）に複製して改変。あおと決めた方針: ①同じサイトの別ページ ②データは通常版と分ける（`fes_`付きの別テーブル。来場者に常時ログ実験メンバーの位置が見えないように＋集計を分けるため） ③18歳以上のまま ④開催時間中だけ使える・アンケート導線・参加人数表示を追加。テーブルは `supabase/fes_tables.sql` で既存テーブルから列・制約・RLSポリシーを読み取って複製する（定義がリポジトリに無いため決め打ちしない）。`push_subscriptions`はEdge Functionが読むので共用。開催日時とアンケートURLは`fes/index.html`先頭の`FES`設定にあり、**現在はテスト用に9/13〜12/31の仮期間・アンケートURL未設定**。ローカルでNode＋ヘッドレスChromeを使い、開始前/開催中/終了間際/終了後の画面表示を確認済み。**SQL未実行のため実データでの動作は未確認**。
- 2026-09-13: あおの指示で、学園祭まではメンバーだけでフェス版を動作確認するため**トップURLをフェス版に転送**する`vercel.json`を追加（`/`と`/index.html`→`/fes/`、一時リダイレクト）。通常版の`index.html`自体は変更していないので、**`vercel.json`を消せば通常版に戻る**。あわせて`FES.alwaysOpen`を追加（`true`の間は開催時間を無視していつでも使える）。**本番前に`alwaysOpen:false`にして`sessions`に本番日時を入れる**こと。注意: この間メンバーのデータは`fes_`テーブルに入るため、常時ログ実験（ロードマップB-1）のすれ違いデータも`fes_encounters`側に貯まる。
- 2026-09-13: あおが`supabase/fes_tables.sql`を本番Supabaseで実行。`fes_`テーブル10個すべて作成され、RLSポリシー数が通常版と一致、通常版テーブルを参照したままのポリシーも0件であることを確認済み。残りはPR作成→mainへのマージ→メンバーでの実機確認。
